{
  "name": "game-of-chance-vercel",
  "version": "1.0.0",
  "description": "Casino game with serverless leaderboard",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/postgres": "^0.5.1"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}